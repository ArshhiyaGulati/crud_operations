A simple and clean CRUD (Create, Read, Update, Delete) application built using React, TypeScript, and Vite.
It displays a list of users, allows adding new users, editing existing users, and deleting users — all with a smooth UI and modal forms.
FEATURES:
Fetches mock users from JSONPlaceholder API
Add new users (Name, Email, Phone)
Edit existing users
 Delete users
 Clean card layout with buttons
 Smooth modal UI for forms
 TypeScript support
 Responsive and simple design

 Technologies Used

React + Vite

TypeScript

CSS (custom styling)

JSONPlaceholder API

LocalStorage
